# Havenwise Home Assistant Integration

## Workflow

After completing changes, always ask the user if they want to:
1. Commit, version bump, and push to git
2. Create a GitHub release (tag + `gh release create`)

Do not push or release without confirmation.
